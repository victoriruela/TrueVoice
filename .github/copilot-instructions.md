⚠️🚨 YOU MUST READ AGENTS.md RIGHT NOW — BEFORE ANYTHING ELSE 🚨⚠️

Before answering ANY question, searching ANY code, or writing ANY code:
1. *READ AGENTS.md at the project root* — it contains the full architecture, DI patterns, and pointers to domain docs.
2. *READ the AGENTS.md inside every folder you are about to touch* — they contain folder-specific rules.

🚨 DO NOT skip this. DO NOT launch exploratory searches before reading these files. The documentation already contains distilled answers — use it FIRST, explore code SECOND.

Those files contain the project architecture, routing rules, conventions, patterns, banned practices, and pointers to per-domain documentation.